<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title> Pets server </title>
    </head>
    <body>
        <h1> List of pets </h1>
        <ul>
            <% for(let i = 0; i < pets.length; i ++){ %>
            <li> <%= pets[i].name %> - <%= pets[i].species %> - <%= pets[i].breed %> </li>
            <% } %>
        </ul>
    </body>
</html>